# Production environment variables for AWS Amplify
# Copy these values to Amplify Console > App Settings > Environment variables
#
# Get values from CDK stack outputs using:
# ./infra/scripts/update-web-env-from-cdk.sh --amplify-format

# API Gateway base URL (no trailing slash)
VITE_API_BASE_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com

# Cognito configuration
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_DOMAIN=enablement-portal-xxxxxxx.auth.us-east-1.amazoncognito.com

# Auth mode (must be 'cognito' for production)
VITE_AUTH_MODE=cognito
